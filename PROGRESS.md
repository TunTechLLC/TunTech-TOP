# TOP — TunTech Operations Platform
## Development Progress
*Read this at the start of every Claude Code session alongside CLAUDE.md*

---

## Current Status

**Phase A (Backend):** ✅ Complete — Checkpoint 1 passed
**Phase B (Frontend):** ✅ Complete — Checkpoint 3 passed 2026-03-30
**Checkpoint 4:** ✅ Passed 2026-04-03 — Dry Run 4 (E004) complete

**Where we are:** The Post-Assembly QA Stage (QA-1–QA-5), the Strengths & Value-Case
Reframe (Tracks 1–3), and the economic-figure-classification fix are built, tested, and
**committed**. The Cobalt E006 dry run then surfaced **systemic signal-layer defects**
(ChatGPT graded v2 7/10), fixed in a six-defect **Signal-Layer Remediation (Phase 1+2:
E, A, A2, D, B, C1; 147 tests)** — all committed and pushed. The active task is the
**Checkpoint 5 re-baseline**: a fresh end-to-end Cobalt run validating the remediation
against the answer key. Full remediation detail in `SIGNAL_LAYER_REMEDIATION_PLAN.md`;
see BACKLOG.md → "CURRENT".

Historical work prior to the current thread lives in `PROGRESS_ARCHIVE.md`.
Test engagement inventory lives in `ARCHITECTURE.md` §"Database Schema".

---

## Active thread — A1–A5 Accuracy and Review series, plus Domain Maturity and Auditor

| Step | Description | Status |
|------|-------------|--------|
| **Checkpoint 4** | Dry Run 4 — new fictional client end-to-end. All 5 agents, Parse Findings, Parse Roadmap, Generate Report. Zero DB Browser operations. | ✅ Passed 2026-04-03 (E004) |
| A1 | Agent Grounding Guards — architectural review (2026-04-24) found DIAGNOSTICIAN_PROMPT and DELIVERY_PROMPT had no hallucination guards while Economics/Skeptic/Synthesizer/Narrator all had explicit CRITICAL/HALLUCINATION PREVENTION blocks. Split `claude.py` (1,785 lines) into `api/services/prompts.py` (all prompt constants + AGENT_REGISTRY) and slimmed `claude.py` (call functions only, imports 5 prompts from prompts.py). GROUNDING RULE blocks added to Diagnostician, Delivery Ops, and Economics prompts simultaneously. Updated `agents.py` (2 local imports) and `patterns.py` (1 local import) to import from `prompts`. 21/21 tests pass. | ✅ |
| A2 Session 1 | Agent Review UI — backend groundwork. `case_packet.py`: `_CONF_PRIORITY` module-level constant; `_section_2_signals()` sorts within each domain High→Medium→Hypothesis (fixes inverted alphabetical-DESC from SQL layer). `agents.py`: `list_agent_runs()` post-processes each run to add `referenced_signal_ids` (bare IDs, brackets stripped) parsed from `output_full` via regex. None-safe. No schema change. 21/21 tests pass. | ✅ |
| A2 Session 2 | Agent Review UI — frontend. `AgentPanel.jsx`: fetches signals on mount (Promise.all with runs); builds `signalMap` keyed by signal_id. Referenced Signals collapsible section below full output: compact cards (ID, name, confidence badge, domain); expands to source file, quote (Quote: '...' regex with U+2014 em-dash separator confirmed), flag button. Ghost signal IDs show red error card. Flag button: pre-populates Consultant Correction textarea, appends if content exists, opens correction section, shows "Flagged ✓" after click. Hint note explains workflow. Consultant Correction moved to bottom of card (below Referenced Signals) so review flows top-to-bottom. Acceptance gate removed — shown on all runs. `agents.py` regex corrected from `\[S\d+\]` to `\bS\d{3,4}\b` (agents write bare S-codes); `dict.fromkeys()` deduplication. 21/21 tests pass. | ✅ |
| A3 Session 1 | Skeptic Recommendations — backend. `PatternRepository`: `UPDATE_CONFIDENCE` SQL + `update_confidence()` method. `patterns.py` router: wire `data.confidence` in `update_pattern()` with 422 guard (was modeled but never implemented). `prompts.py`: `DOWNGRADE_EXTRACTION_PROMPT` constant. `claude.py`: `extract_downgrade_recommendations()` — max_tokens=500, validates P-code format and confidence values, returns [] on failure. `agents.py`: `_parse_c_codes()` regex helper (deduplicates signal_ids via dict.fromkeys); `GET /skeptic/c-codes` (pure regex, no Claude); `POST /skeptic/parse-recommendations` (Claude + regex, in_engagement flag per downgrade, library name lookup for unmatched patterns). `api.js`: `getSkepticCCodes`, `parseSkepticRecommendations`. 21/21 tests pass. | ✅ |
| A3 Session 2 | Skeptic Recommendations — PatternPanel downgrade cards. "Parse Skeptic Recommendations" button (amber outline, shown only when Skeptic accepted and recs not yet fetched). Downgrade cards: P-code, pattern name, confidence arrow (High→Medium), reason sentence, Apply/Dismiss. Apply writes updated confidence via PATCH endpoint, refreshes pattern list, removes card. Dismiss removes card for session only. `in_engagement:false` cards shown greyed with disabled Apply and italic note. `visibleDowngrades` filters dismissed IDs and already-applied (current_confidence === recommended_confidence) so applied cards never reappear after reload. Empty states: no recs found, all applied/dismissed. 21/21 tests pass. | ✅ |
| A3 Session 3 | Skeptic Recommendations — SignalPanel C-code badges. `fetchData()` adds `getSkepticCCodes` to Promise.all (auto-fetches on mount, no button needed). Builds `cCodesBySignalId` map keyed by signal_id. Amber badge per C-code on each flagged signal card (⚠ C001 · factual_conflict). Click to expand full C-code text inline as monospace block; click again to collapse. Multiple C-codes per signal render as separate independently expandable badges. Bug fix: `_parse_c_codes` deduplicates signal_ids via dict.fromkeys to prevent duplicate React keys when Signal A and B reference the same signal. 21/21 tests pass. | ✅ |
| A4-Pre | Pattern Detection: Include Triggering Signal IDs in Notes — `PATTERN_DETECTION_PROMPT` updated: `notes` field now requires `Triggered by: S014, S022` as first line, followed by explanation. Uses only signal IDs from SIGNALS input — no pattern library IDs or invented codes. Prerequisite for A4 evidence chain. Bug fix committed alongside: `load_patterns` endpoint now calls `delete_unaccepted_for_engagement()` before `bulk_create()` — replaces instead of appending on re-detection. 21/21 tests pass. | ✅ |
| A4 | Evidence Traceability on Findings — `_build_evidence_chain()` added to `findings.py`: extracts S-codes from pattern notes `Triggered by:` line, resolves each to signal record, extracts verbatim quote (splits on ` Interpretation:`, strips `Quote:` prefix), caps at 8 signals with `signals_hidden` count for overflow note. `signals_by_id` dict built from `all_signals` (already fetched). `evidence_chain` key added to each candidate in `parse-synthesizer`. `FindingsPanel.jsx`: `expandedEvidence` Set state, `toggleEvidence` handler, collapsible "Evidence Chain (N patterns, N signals)" toggle at bottom of each candidate card. Expanded: pattern rows with `→ S-codes` arrows, signal rows with name · confidence · quote · [source_file], `+ N more signals not shown` when capped. Candidates only — not saved findings list. 21/21 tests pass. | ✅ |
| Backlog | Domain Maturity Scoring — `_compute_domain_scores()` in `report_sections.py`: base 5, subtract per accepted pattern (High: -1.0, Medium: -0.5, Hypothesis: -0.25) and per finding (High: -0.5, Medium: -0.25), floor at 1.0, round to 1dp. All 10 VALID_DOMAINS shown; None for zero signals+patterns+findings. `_maturity_scorecard()` renders two-column table (Domain / Maturity Score 1–5) with traffic-light cell shading (green ≥4.0 Healthy, yellow ≥3.0 Concern, red <3.0 Critical, gray No Data). Placed in Section 2 after engagement overview paragraph. `PatternRepository` added to `report_generator.py` generate(). 21/21 tests pass. Validated on E004. | ✅ |
| A5 | Signal ID Validation on Agent Output — `SignalRepository.get_ids_for_engagement()` returns set of valid signal IDs for an engagement. `list_agent_runs()` in `agents.py` fetches valid IDs once per request, adds `referenced_signal_ids` (all S-codes found in output_full) and `signal_warnings` (ghost IDs not in valid set) to each run. Ghost warnings logged at WARNING level. `AgentPanel.jsx`: yellow warning banner visible on collapsed cards between error block and output summary — shows unknown ID(s) and prompts review before accepting. Does not block acceptance. 21/21 tests pass. | ✅ |
| Backlog | Narrator Output Auditor Session 1 — `api/services/narrator_auditor.py` with 12 mechanical Python checks on raw narrator JSON before Word render: (1) R-codes in prose resolve to real roadmap items, (2) finding IDs in executive_briefing + F-codes in prose resolve to real findings, (3) economic figures match a DB structured field within 5% tolerance OR carry CONFIRMED/DERIVED/INFERRED label nearby, (4) root_cause_narrative exactly 4 paragraphs, (5) economic_impact_narrative ≤2 sentences, (6) executive_snapshot ≤3 sentences and each ≤20 words, (7) success_metric uses "Track: ... Complete: ..." format, (8) domain narratives ≤3 sentences each, (9) Quick Wins items satisfy High+Low criteria, (10) signal coverage map present, (11) Rule 1 — concentration stabilization not placed in Scale, (12) individual names in risk rows (potential HR exposure, heuristic). `AuditResult` dataclass + `AuditContext` (findings/roadmap/processed_files/signals/coverage/firm_name) + tuple registry. `ReportGeneratorService.generate()` returns `{saved_to, audit}` — audit runs between narrator and Word render. `/report/generate` endpoint returns both. `download_report` updated to unpack new shape. ReportPanel.jsx renders Trust Report panel between Generate button and saved-path box: green/red/gray dimension icons, summary header, expandable evidence on flagged dimensions, localStorage persistence, failed dimensions auto-expand on fresh run. Two post-test hardening passes on false positives: (1) dollar regex tightened with `(?![A-Za-z])` lookahead so K/M/B suffix doesn't consume the next word's first letter (e.g. "$13 below" → "$13", not "$13 b"), figures under $1,000 skipped (unit rates), figures with "per hour"/"per X" suffix skipped, all three evidentiary labels accepted (was DERIVED-only); (2) individual-name extraction requires person-verb context in signal notes, then strips candidates appearing in finding text or in org-suffix patterns (X account/engagement/escalation/etc.). 57/57 tests pass (36 new). Audit-only endpoint deferred — would require caching narrator output (schema change). | ✅ |
| QA-1 | Post-Assembly QA Stage — Coverage Check Agent shipped end-to-end. New table `QACoverageItems` (migration `migrations/migrate_qa_coverage.py`). New `QACoverageRepository` with sequential-loop `bulk_create()`, `update_status`, `batch_accept_tier_1` (respects explicit rejection), `delete_for_engagement` (re-detection replaces). New `next_qa_coverage_id()` with prefix `QC`. New `QA_COVERAGE_PROMPT` in `prompts.py` with tier rubric and two worked examples drawn from verified E004 source. New `detect_coverage_gaps()` in `claude.py` — async, model parameter explicitly `claude-opus-4-7` (overrides global `TOP_MODEL` Sonnet default for QA agents only), streams output via `async_client.messages.stream()` (required — first non-streaming attempt timed out at 5 minutes per Anthropic's long-request guidance). New `api/services/qa_inputs.py` with `read_v1_roadmap_text()` and `assemble_source_documents_block()` — reuses existing `extract_text_from_file()`. New `api/routers/qa_coverage.py` — POST run / GET list / PATCH status / POST confirm-tier-1. Frontend: `qaCoverage` in `api.js`; new `QACoveragePanel` with tiered UI (Tier 1 collapsed with batch confirm, Tier 2 expanded, Tier 3 collapsed). Whole-card status visuals (left border + background + button state) added after consultant UX feedback that initial styling was unclear. Bugs found and fixed during build: `docs_folder` → `documents_folder` actual column name; source files split across `interviews_folder` and `documents_folder` (added prefix-based resolver); migration print Unicode → ASCII for Windows cp1252 subprocesses; non-streaming → streaming for long Opus calls. E004 smoke test: 34 items (T1: 10, T2: 21, T3: 3) — all source-grounded with file:line citations; QC003 caught the P14 "this week" PM assignment urgency that v43 dropped from Priority Zero. 62/62 tests pass (5 new). | ✅ |
| QA-2 | Post-Assembly QA Stage — Coherence Check Agent shipped end-to-end. New table `QACoherenceItems` with `sections_involved` stored as JSON in TEXT column (same pattern as `findings.key_quotes`). New `QACoherenceRepository` with JSON serialize/deserialize on get/bulk_create. New `next_qa_coherence_id()` with prefix `QH` (H = coHerence — distinct from QC). New `QA_COHERENCE_PROMPT` with category enum (contradiction/priority_mismatch/weak_grounding/missing_root_cause), tier rubric, worked examples (the $186K mislabel and Customer Experience priority mismatch — both from verified Cowork Internal Audit). New `detect_coherence_issues()` using Opus 4.7 streaming. **Architecture deviation from BACKLOG (flagged before build):** does NOT pass accepted QA-1 items as context — cleaner standalone read matching Cowork's actual workflow; "do not generate X" instructions are fragile in LLM prompts and the agents check different dimensions anyway. New router `qa_coherence.py` (run/list/update/confirm-tier-1). New `QACoherencePanel` with category badges (contradiction red, priority_mismatch orange, weak_grounding yellow, missing_root_cause purple), sections_involved rendered as monospace chip tags, recommended_fix in amber block. E004 smoke test: 10 items (T1: 5, T2: 4, T3: 1; 7 contradiction, 2 priority_mismatch, 1 missing_root_cause, 0 weak_grounding). All 5 Tier 1 items are real objective errors — QH001 caught the $186K mislabel in the Executive Briefing (the most embarrassing v43 error per the verified Cowork reference). 67/67 tests pass (5 new). | ✅ |
| QA-3 | Post-Assembly QA Stage — Editorial Check (split implementation) shipped end-to-end. Two pipelines orchestrated by a single agent. **Python pipeline:** NEW module `api/services/editorial_auditor.py` (NOT extending `narrator_auditor.py` — BACKLOG language refined during build; the two auditors have different inputs/timing: narrator_auditor reads JSON pre-render, editorial_auditor reads rendered text). Three MVP checks: signal codes leaking (`\bS\d{3,4}\b` regex; P/F/R codes skipped to avoid collision with client project IDs like P10/P14), undefined PMO/SOW at first use (small fixed acronym list, document-wide first-use detection), Operations Manager vs Director of Operations co-occurrence (terminology drift naming the standard term). **Claude pipeline:** new `QA_EDITORIAL_VOICE_PROMPT` + `detect_editorial_voice()` — narrow scope (voice intrusions, tense/register shifts without transition, CEO-audience appropriateness), prompt explicitly excludes scope of the Python pipeline to avoid duplication. New table `QAEditorialItems` with `source` ('python'\|'claude') and `standard_term` columns. New `QAEditorialRepository`, ID generator (`QE` prefix), router orchestrating both pipelines (Python first — fast/deterministic, then Claude — network-bound). New `QAEditorialPanel` with PYTHON/CLAUDE source badges (slate-mono / violet) so consultant knows which pipeline flagged each item. E004 smoke test: 9 items (T1: 8, T2: 1, T3: 0; Python: 6, Claude: 3). Python caught S217, S237, S228 (bonus catch over Cowork which only flagged S217/S237), PMO and SOW acronym issues, and the role drift (Operations Manager 10× vs Director of Operations 3×). Claude caught the Path 3 vendor-pitch tone (Cowork D1), the future-state tense shift (Cowork D2), and a bonus Executive Briefing implementation-language phrasing the CEO wouldn't parse. **MVP scope** deferred: numeric inconsistencies (overlaps QA-2), broken section refs, evidentiary label legend, British/American spelling — add if Checkpoint 5 shows demand. 76/76 tests pass (9 new — 5 repository + 4 Python checker function tests). | ✅ |
| QA-4 | Post-Assembly QA Stage — Revision Agent shipped end-to-end (backend + API; UI deferred to QA-5). Single Opus 4.7 call returns a structured **edit list** (not a rewritten document); TOP applies the edits to the v1 `.docx` **in place** and saves `_v2.docx` — v1 is never modified, so the v1↔v2 diff is exactly the QA contribution. **Architecture reversal (vs BACKLOG's "complete document, not a list of edits"):** diff analysis of the v44 ChatGPT reference showed it was an in-place edit of v43 (72% identical, all 29 tables preserved, **0 deletions**), proving in-place editing — not regeneration — is correct; regeneration would pollute the diff and defeat the comparison goal. **Model: Opus 4.7 locked** via a throwaway Step 0 empirical test against the real E004 v43+accepted-items input: 4.7 (hardened prompt) hit 97% clean anchor applicability and auto-applied 35 edits vs 4.8's 91%/21 — 4.8 over-flagged simple edits as `manual`; since the mandatory QA-5 diff review is the safety backstop, maximal correct automation wins. New: `QARevisionEdits` table (one row per edit + outcome + `source_item_id`; migration `migrate_qa_revision.py`, `QR` prefix), `QARevisionRepository`, `QA_REVISION_PROMPT`, `generate_revision_edits()` (streaming, `QA_REVISION_MAX_TOKENS=32000` per-call — no global change), `assemble_accepted_qa_items()` (block + accepted-item manifest), `api/services/qa_revision.py` (tolerant matcher exact→`context_before`→fuzzy→**flag**; run-level replace preserving formatting; insert-after; **never silently skips or corrupts**; cross-paragraph/ambiguous/not-found all flagged), router `qa_revision.py` (run / list / v2-download / patch-outcome). Report Generator output renamed `_v1.docx` in lockstep (`qa_inputs` templates updated). **Reconciliation (closes the silent-omission gap the diff review cannot catch):** every edit carries `source_item_id`; accepted items with no edit are recorded `unaddressed` and surfaced in the revision note + worklist. **Table fix proven via prompt only** (single-cell-anchor guidance) — no cell-aware applier code needed. E004 smoke (hardened): 49 accepted → 29 applied (incl. the $186K mislabel, $271K floor, 6–7wk lag, three-vs-four engagements), 5 flagged, 3 manual, **19 unaddressed surfaced** (previously invisible — ~4 are Claude "covered-by-sibling" placeholders, safe over-surfacing); v1↔v2 line similarity 0.834; zero false applies. 93/93 tests pass (17 new — repository + pure matcher/applier + reconciliation). **Post-ship fix (2026-06-01, found during QA-5 review of the real E004 v2 — §10.7/10.8 corruption):** `insert_paragraph_after` collapsed multi-line `new_text` into one run AND inherited the anchor paragraph's style, so QR037 (anchor = the "Initiative Dependencies" heading) rendered a whole new section as one giant Heading-2 blob wedged between the heading and its table. Three fixes: (1) split `new_text` on newlines into one body paragraph each; (2) inserted paragraphs use `Normal` style, never inherit a heading; (3) a multi-paragraph insert whose anchor resolves to a heading is now **flagged** (`match_method='insert_under_heading'`) for manual placement rather than applied. The "never silently corrupts" guarantee had covered anchor *matching* only, not insert *structure* — this closes that gap. 98/98 tests pass (5 new). The existing broken v2 on disk must be regenerated by re-running the revision. | ✅ |
| QA-5 | Post-Assembly QA Stage — integrated QA Tab UI shipped end-to-end (frontend + 2 thin backend endpoints). The three separate `qa1`/`qa2`/`qa3` tabs collapse into **one `QA` tab** (`QAPanel.jsx`); the three detection panels are **wrapped, not rebuilt** (lowest regression risk) — each gains one additive optional `onCountsChange` prop (fires on `items` change; no-op standalone) so the parent shows live per-agent counts in collapsible section headers without lifting state or double-fetching. **Gate:** new `GET /{id}/qa-status` (in `qa_revision.py`, pure `os.path.exists` on v1/v2 — no generation) drives the tab gate; if no v1, the tab shows "Generate the roadmap first" and stops. **v1 download fix:** added `GET /qa-revision/v1` serving the *saved* file off disk — `/report/download` re-generates on every call and would desync the comparison from the v1 the agents actually analyzed. **Revision = verify-after** (consultant choice): the edits apply to produce v2 first, then `QAPanel` renders the `QARevisionEdits` rows grouped by outcome (applied / needs-your-hand / manual / verify-coverage / handled), before→after from `anchor`→`new_text`, `source_item_id` chip, `reason`, and a `match_method` badge with non-exact applies sorted to the top of "applied" so attention goes where confidence is lower. Orientation line ("N applied (X exact · Y to glance) · …") sets effort expectations. "Run Final Revision" gates on ≥1 accepted item across the three agents. Per-edit "Mark handled" → `PATCH outcome:'manual_done'` so the consultant reaches a defined endpoint (Checkpoint 5 criterion). Download v1 / Download v2 buttons for the authoritative Word side-by-side (in-app list is the primary comparison — downloads optional). **Architecture deviations flagged before build & confirmed:** (1) comparison is the structured edit-list, NOT a raw docx text-diff — a text-diff would reintroduce the rendering noise QA-4's in-place-edit design eliminated; (2) soft sequencing only, no hard gating between agents — the BACKLOG's "Coherence uses accepted Coverage items" rationale is stale (QA-2 as-built is a standalone read). `qaRevision` + `qaStatus` added to `api.js`. 93/93 tests pass (no new — the 2 endpoints are trivial file-existence checks with no business logic; no `TestClient` harness exists in the suite, so per the project test strategy they're verified in the manual app run rather than by inventing a new test pattern). Frontend build clean. Plus the QA-4 §10.7/10.8 applier fix above, found by reviewing the real v2 this UI exposed. | ✅ |
| Strengths T1 | **Strengths & Value-Case Reframe — Track 1** (built 2026-06-11, committed 6c139e5). Valence as a field: `migrate_valence.py` (nullable `valence` on Signals + OPDFindings; NULL≡Negative); models/repos/`domains.py`/`constants.js`; all 7 signal-extraction prompts emit `Strength/Risk/Neutral`; case packet surfaces valence to Skeptic/Synthesizer. `FINDINGS_EXTRACTION`/`SKEPTIC`/`SYNTHESIZER` prompts emit/challenge/assemble strengths; `Positive/Dual/Negative` finding valence. `findings.py`: 422 relaxed for Positive (require ≥1 domain Strength signal, not a pattern); `_build_strength_evidence_chain`; valence-aware evidence summary. `report_sections.py`: scorecard skips subtraction for Positive/Dual; new `_what_to_preserve` section before the roadmap; Positive/Dual excluded from Domain Analysis; render-time `compute_section_map` (shifts numbers only when the section renders → negatives byte-identical). | ✅ |
| Strengths T2 | **Track 2** — `executive_summary_strengths` narrator field ("what's working / right to win" thread), fed by a VALIDATED STRENGTHS block in `claude.py`; rendered after summary para3, omitted when null (byte-identical when empty); added to compress pass + narrator-auditor prose list. | ✅ |
| Strengths T3 | **Track 3** — Executive Brief value reframe (**reframe, not restructure**): value-first `executive_snapshot` (prize → structural cause → recoverable value → first move), whole-brief VALUE-CASE POSTURE, "Three Critical Problems" → "Three Gaps to Close", roadmap `roadmap_rationale` value spine. Skill: strengths evidence rule + Preserve-finding exception; QA-2 `weak_grounding` catches generic praise. | ✅ |
| Econ figure classification | **Root-cause fix for executive economic figures (2026-06-12).** `figure_type` was assigned by a domain-lookup table, mis-routing the "Revenue at Risk" headline — E006 v2 showed **$127K** (PMO opportunity cost) instead of the **$2.8M** Helix exposure (mirrors E004's $186K). Now classified from the figure's *nature*: `_classify_figure_type` reads the economic_impact text the agent wrote (exposure/drag/opportunity/replacement signals, scoped to the primary figure's window so a secondary figure can't capture the type) and valence (Positive/Dual → `funding_capacity`); the domain map is demoted to a last-resort fallback. `_suggest_economic_figures` blanks the exposure columns for strengths; the findings figure extractor (`_parse_economic_figures`) skips unit-rate/sub-$1K figures (F007 "$100 per violation" → no figure). New pre-render guard `check_revenue_at_risk_coherence` (narrator_auditor) flags a "Revenue at Risk" headline materially dwarfed by a larger executive exposure. Item-2: valence exclusion completed on the economic exposure table + chart (a strength no longer renders as a "drag"). Frontend: `funding_capacity` Type option + valence-aware default. 126 tests (+16). E004 (negatives-only) byte-unchanged — all changes are parse-time or valence-gated. | ✅ |
| Cobalt fixes | **Bug-fix bundle found during the Cobalt E006 dry run** (all tested): valence-blind domain cap now reserves up to 2 Strength slots/domain; valence casing normalization + coercion/observability logging (strength-survival + Positive/Dual counts); agent `call_claude` cap 8k→16k, narrator 24k, roadmap extraction 16k, all with `stop_reason=='max_tokens'` truncation warnings (**3 of 5 Cobalt agents had silently truncated at 8k**); `AgentRunRepository.accept()` enforces one accepted run per agent (re-runs left two accepted → `get_accepted_output` non-deterministic on same-day ties) + `run_id` tiebreaker; frontend Preserve-finding exempt from the ≥1-pattern load guard; test-fixture drift fixed (Signals/OPDFindings/AgentRuns). New `scripts/valence_state.py` checkpoint. `top.log` untracked + gitignored. 110 tests pass. | ✅ |
| Signal-Layer Remediation (Phase 1+2) | **Systemic signal-quality fixes found during the Cobalt E006 dry run** (ChatGPT graded v2 7/10; root cause = the signal layer, not one client). Six defects, each evidence-gated with a pre-registered test, all committed + pushed: **E** (`4c0b058`) standardize on Opus 4.7 + stream all 7 long Claude calls (no timeouts); **A** (`3d45cce`) JSON robustness — malformed extraction JSON no longer silently stores zero signals (had lost the CTO + Operations interviews); retry-then-fail-loud, failed file re-attempted next run, SignalPanel surfaces it; **A2** (`1ec8329`) QA agents + narrator fail loud — a parse failure can no longer read as "0 issues = clean"; **D** (`13b1093`) `ECONOMICS_PROMPT` precedence/labeling — exposure is DERIVED/"risk not loss", never CONFIRMED (fixes the E3 mislabel); **B** (`dc93ee3`) semantic dedup of cross-file restatements (concentration ×6→1; corroborates to High; within-domain enforced in code; `_reconcile_partition` tolerates model index slips; graceful flagged fallback); **C1** (`f7c02e7`) quality-gated selection replacing the fixed per-domain cap — keep ALL High/domain, top-3 Medium (corroboration-ranked, strength reserve), global ceiling 55 worst-first with per-domain floor. Economics fixed end-to-end (B→C1→D): on E006, 120 raw → 65 deduped → 49 main, E1 AND E2 reach the agent. 147 tests. C2 (single-source derived demotion) deferred to the re-baseline. Full record: `SIGNAL_LAYER_REMEDIATION_PLAN.md`. | ✅ |

---

## Next Steps

**Current task — Checkpoint 5 re-baseline (Cobalt, fresh engagement).** The Signal-Layer
Remediation (Phase 1+2: E, A, A2, D, B, C1) is complete, committed, and pushed (147 tests).
The next step is the comprehensive end-to-end re-baseline:

1. Create a **fresh engagement** on the Cobalt source (do NOT wipe E006 — it is the documented
   "before"). Run it end-to-end: process files → patterns → agents → findings → roadmap →
   report → QA. Grade v1/v2 against the answer key.
2. **Confirm:** E1/E2 economics appear with correct labels; Revenue-at-Risk is the Helix
   exposure (DERIVED, not CONFIRMED); material signals (concentration, PMO/authority, renewal)
   survive; no silent file loss; the main candidate set is bounded (~49 on E006) and reads as
   the right signals.
3. **Make the deferred C2 call:** does any *material single-source derived* figure still get
   demoted to Hypothesis-and-hidden? If yes → add a derived-reserve in selection (safer than a
   rubric change). If no → C2 not needed. Then close Checkpoint 5 against the pass criteria in
   BACKLOG.md.

Resume pointer + full remediation evidence: `SIGNAL_LAYER_REMEDIATION_PLAN.md` → "▶ RESUME HERE".
