# TOP — TunTech Operations Platform
## Development Progress
*Read this at the start of every Claude Code session alongside CLAUDE.md*

---

## Current Status

**Phase A (Backend):** ✅ Complete — Checkpoint 1 passed
**Phase B (Frontend):** ✅ Complete — Checkpoint 3 passed 2026-03-30
**Checkpoint 4:** ✅ Passed 2026-04-03 — Dry Run 4 (E004) complete

**Where we are:** Post-Checkpoint 4 backlog work in progress. See BACKLOG.md for After
Checkpoint 4 items.

---

## Completed Steps

| Step | Description | Status |
|------|-------------|--------|
| 1–3 | Database schema, 14 tables, repositories, API utils | ✅ |
| 4 | FastAPI app, 8 routers, CORS, logging, error handler | ✅ |
| 5 | Dashboard + NewEngagement components | ✅ |
| 6 | EngagementDetail, 8 tabs, Edit Settings | ✅ |
| 7 | All panel components (Signal, Pattern, Agent, Finding, Roadmap, Knowledge, Report) | ✅ |
| 8 | Document processing pipeline | ✅ |
| 8 Ext 1 | File processing and signal extraction | ✅ |
| 8 Ext 1 Cleanup | Multi-file review fix (source_file label, parallel fetch) | ✅ |
| 8 Ext 1 Cleanup | File type expansion — STATUS and RESOURCE prompts (DELIVERY deferred) | ✅ |
| 8 Ext 1 Cleanup | Auto-cull signal candidates — dedup, domain cap, hypothesis filter/toggle | ✅ |
| 8 Ext 2 | Synthesizer-to-Findings parser | ✅ |
| 9 | Cross-engagement report screen | ✅ |
| 10 | Word report download | ✅ |
| Backlog | Report Narrator — narrative prose layer for OPD report | ✅ Validated (E002, E003) |
| Backlog | Synthesizer-to-Roadmap Parser — detect-review-load for roadmap items | ✅ Validated (E002) |
| Backlog | Add field labels to Finding candidate review cards | ✅ |
| Backlog | Disable agent buttons while any agent is running | ✅ |
| Backlog | Roadmap item edit and delete | ✅ |
| Backlog | Word report template — custom .docx, table column widths, header shading | ✅ |
| Backlog | DELIVERY_DOCUMENT_EXTRACTION_PROMPT — risk registers, retrospectives, portfolio summaries, proposals | ✅ |
| Backlog | Signal candidate labels — field labels added to candidate review cards | ✅ |
| Backlog | Reprocess button — source_file column on Signals, delete+reprocess endpoint, processed files UI | ✅ |
| Backlog | Candidate file cleanup — archive merged and individual candidate files to processed/ after loading | ✅ |
| Backlog | Engagement header count refresh — onRefresh callback from EngagementDetail to all panels | ✅ |
| Backlog | Replace report download with save-and-show-path — generate endpoint, open-folder endpoint | ✅ |
| Backlog | OPD report major restructure — Sections 5-9 rebuilt, new Sections 7 / 8.1-8.7 / 9 | ✅ Validated (E003) |
| Backlog | Report Narrator — rewrite to JSON output, 8 new structured sections (future state, priority zero, roadmap overview, initiative details, dependencies, risks, next steps) | ✅ |
| Backlog | ROADMAP_EXTRACTION_PROMPT — owner field with role-based derivation and "TBD — assign at kickoff" fallback | ✅ |
| Backlog | Section 6 economic summary table — _parse_economic_figures() parses CONFIRMED/INFERRED from free text, primary figure per cell, clean totals row from Consulting Economics finding | ✅ |
| Backlog | ReportPanel — remove stale sections list, persist last saved path via localStorage | ✅ |
| Backlog | Word report template — additional formatting cleanup (heading numbers, economic table, section structure verified on E003) | ✅ |
| Backlog | Economic Impact Reasoning — FINDINGS_EXTRACTION_PROMPT updated to require inline calculation reasoning (method, inputs, source per figure) | ✅ Validated (E003) |
| Backlog | PATTERN_DETECTION_PROMPT — send full pattern library to Claude at detection time; add domain coverage instruction. Root cause: Claude was guessing P-IDs without library context, causing new domains (AI Readiness, HR, Finance) to be missed. Validated: 40 patterns detected on E003 including P48–P53, P57, P59, P60 | ✅ |
| Backlog | Signal confidence accuracy — replaced self-assessed confidence rules with mandatory evidence_quality field across all 7 extraction prompts (SIGNAL_EXTRACTION_PROMPT + 6 document prompts). Forces Claude to actively scan for [unclear]/[inaudible] markers, contradictions, hedging, and interviewer inferences before assigning confidence. Root cause: Claude was rating confidence on its own pre-selected best quote, not on the full evidence picture. Validated on 12-file test set: 4 Hypothesis signals produced (previously 0), correct High/Medium distribution on remainder | ✅ |
| Backlog | Finding priority accuracy — replaced open-ended priority field with explicit derivation criteria in FINDINGS_EXTRACTION_PROMPT. High = active margin bleed / structural blocker / CONFIRMED economic impact; Medium = operational improvement / INFERRED impact; Low = capability/maturity improvement with no direct economic impact. Validated on E004: 7 High, 3 Medium (previously 9 High, 1 Medium) | ✅ |
| Backlog | Roadmap owner field bug — owner was generated by Claude but dropped from the load payload in RoadmapPanel.jsx and not shown on candidate review cards. Fixed: owner field added to candidate card (editable) and to the create payload in handleLoadRoadmapItems | ✅ |
| Backlog | Multi-audience report redesign — Exec Summary restructured (opening para + Key Findings box + 3 short paras); How to Read This Document prefatory page; Engagement Overview redesigned (3-line metadata + narrator paragraph); Section 3 renamed to Operational Maturity Overview; domain audience callouts (small italic) on each domain in Section 4; Section 9 shifted to execution voice | ✅ |
| Backlog | File naming convention — parse_file_role_and_type() replacing fragile regex role extraction; Interview_/Doc_ prefix convention; ordered role/doc-type maps; _Followup and _N suffix stripping; no "team member" fallback; documented in CLAUDE.md | ✅ |
| Backlog | Report fixes — How to Read heading style (TOC-visible, section number suppressed, page break before); Primary Cause full first sentence (no 120-char hard cap); Revenue at Risk sourced from Sales domain confirmed figure; 18-Month Gross Margin Target row with current baseline; totals row excludes concentration/relationship-risk findings; R-code resolution in dependency table | ✅ |
| **Checkpoint 4** | Dry Run 4 — new fictional client end-to-end. All 5 agents, Parse Findings, Parse Roadmap, Generate Report. Zero DB Browser operations. | ✅ Passed 2026-04-03 (E004) |
| Backlog | Executive Briefing — one-page CEO teaser before Executive Summary. Narrator generates executive_briefing JSON object (headline, problems[], numbers[]). report_generator.py assembles page with hallucination-proof validation: finding_ids checked against DB, figures sourced from DB via _parse_economic_figures(). Styled block headers (all caps, 11pt, spaced), horizontal rule after headline, page break after page. | ✅ |
| Backlog | Consultant Voice Compression — compress_narrative() in claude.py post-processes Executive Summary (4 prose strings) and Section 9 completion_criteria after Narrator call. Parallel asyncio.gather; falls back to original on any failure. Target 25-30% reduction. | ✅ |
| Backlog | Pattern Name on Candidate Review Cards — detect endpoint enriches response with pattern_name (consolidated duplicate get_library() call). PatternPanel.jsx shows name between ID and confidence badge on candidate cards. | ✅ |
| Backlog | Visual 1 — Economic Breakdown Chart — horizontal bar chart of confirmed exposures by finding, embedded in Section 6 before the economic summary table. Generated with matplotlib (Agg backend), temp PNG deleted after embedding. Same seen_confirmed dedup logic as the economic table. | ✅ |
| Backlog | Reader guide dynamic section numbers — _SECTION_MAP dict in report_generator.py replaces hardcoded section numbers in _ROLE_READING_GUIDE. All priority/detail strings use format placeholders resolved at render time via .format(s=_SECTION_MAP). One place to update when numbering shifts. | ✅ |
| Backlog | Accuracy guardrail pass — (1) Narrator section refs now dynamic: _SECTION_MAP in report_generator.py builds section_refs dict passed to generate_report_narrative(); prompt references SECTION REFERENCES block in input, never hardcodes numbers. (2) Replaced specific names in completion_criteria example with generic role/project placeholders — eliminates cross-client contamination risk. (3) opd_section prompt and validation corrected to match current 9-section report structure. (4) bulk_create() in signal.py fixed: list comprehension → sequential loop; added missing source_file field. (5) Stale delivery comment in document_processor.py corrected. | ✅ |
| Backlog | Consultant Correction on Agent Outputs — consultant_correction TEXT column on AgentRuns. AgentRunRepository: GET queries include new field; get_prior_output() returns output_full + correction block if present; update_correction() PATCH. Router: run_agent() uses get_prior_output() for downstream context; new PATCH /{run_id}/correction endpoint. Frontend: AgentPanel collapsible correction section on accepted runs with Active badge, save/saved state. get_accepted_output() unchanged — prerequisite validation, report generation, findings/roadmap parse all correctly bypass corrections. | ✅ |
| Backlog | Visual 2 — Roadmap Timeline — Gantt-style horizontal bar chart embedded at start of Section 8. Phase zone background shading. Bars colored by phase. Timeline sourced from narrator initiative_details keyed by item_id; falls back to phase-level defaults per item when narrator detail is absent so chart always renders. Initiative names truncated at 35 chars. Legend shows only phases present in the roadmap. Temp PNG deleted after embedding. | ✅ |
| Backlog | Signal confidence — corroboration upgrade at merge — _deduplicate_candidates() in document_processor.py now tracks a corroborated set. When two files independently extract the same signal (same domain + signal_name key), surviving candidate's confidence is upgraded one level: Medium→High, Hypothesis→Medium. High stays High. logger.debug records each upgrade. No schema change, no Claude call — purely algorithmic. | ✅ |
| Backlog | Report fix — Named PM anonymization: ROADMAP_EXTRACTION_PROMPT OWNER RULES extended to prohibit individual names as owners. REPORT_NARRATOR_PROMPT — new PRIVACY/ANONYMIZATION section: named ICs never appear in performance contexts; role references and PM-A/PM-B notation used instead. Does not apply to named client contacts or commercial project references. | ✅ |
| Backlog | Report fix — Cover page distribution restriction: _add_cover_page_restriction() scans doc.paragraphs for "Confidential" line and inserts "Distribution: Restricted — Contains individual performance assessment data..." immediately after it, copying paragraph and run formatting from source. Always present regardless of whether named individuals appear. Logs warning gracefully if line not found. | ✅ |
| Backlog | Report fix — Reader guide dynamic domain names: _ROLE_READING_GUIDE entries now use {domain_clause} placeholder instead of hardcoded domain names in priority strings. domain_order list added per entry. _how_to_read_page() computes domain_clause as intersection of domain_order and finding_domains — only domains with accepted findings in the current engagement appear. Customer Experience added to Director of Delivery's domain list. Works for any engagement. | ✅ |
| Backlog | Report fix — Shared exposure cell labels: _economic_summary_table() duplicate confirmed figures now display as "$X — shared, see note" instead of "$X*". Footnote explanation below table unchanged. Detection logic (seen_confirmed dict) unchanged — only the cell display value updated. | ✅ |
| Backlog | Report fix — Key risks count expanded: risk_table_rows instruction in REPORT_NARRATOR_PROMPT updated from max 3 to 3–6 rows. Four risk categories required (adoption, capacity, evidence gap, organizational dynamics). Every risk must be grounded in a specific signal, finding, or Skeptic-flagged uncertainty. No individual IC names in risk descriptions. _risk_table() renderer already renders all rows — no renderer change needed. | ✅ |
| Backlog | Report fix — Future State CEO day sentence: future_state_narrative instruction updated to require exactly one sentence describing what the CEO's day looks like 18 months out — specifically what they are no longer doing. Must be grounded in confirmed signals about current CEO time consumption. Omitted rather than fabricated if no CEO time signals confirmed. | ✅ |
| Ad hoc | DERIVED economic figure label — third notation alongside CONFIRMED/INFERRED. Applies when a dollar figure is the arithmetic result of confirmed inputs but was never stated in any source document. FINDINGS_EXTRACTION_PROMPT updated with definition, classification rule, and updated priority derivation (CONFIRMED or DERIVED → High). All prompts updated (CONFIRMED/DERIVED/INFERRED throughout). _parse_economic_figures() returns 3-tuple; Section 6 economic table expanded to 5 columns (Derived Exposure added); Visual 1 bar chart includes DERIVED bars in steel blue with legend; Executive Briefing accepts DERIVED with "Calculated from:" disclosure. | ✅ |
| Ad hoc | Skeptic contradiction detection — Section 6 added to SKEPTIC_PROMPT. Four types: factual_conflict (with document vs. interview precedence rules), retraction (later statement operative), role_discrepancy (interviewee self-stated title authoritative), second_hand_attribution (unconfirmed third-party attribution flagged with C-code). SYNTHESIZER_PROMPT Section 1 extended to require resolution of all C-codes before generating findings. source_file added to case packet signal lines as prerequisite for cross-document identification. | ✅ |
| Ad hoc | Synthesizer attribution verification gate — silent pre-finalization check in SYNTHESIZER_PROMPT before any finding referencing a named individual. Three rules: title accuracy (self-stated title from own transcript; cross-references role_discrepancy C-codes), statement traceability (must trace to person's own interview; cross-references second_hand_attribution C-codes), second-hand disclosure ("per [Role]'s account" language required; sole second-hand basis downgrades High → Medium confidence). No output section — corrections applied silently. Applies to all engagements. | ✅ |
| Ad hoc | scan_folder engagement_id prefix filter removed — dedicated per-engagement folders make the prefix check redundant. Files no longer required to start with engagement ID (e.g. E004_). Hash-based duplicate detection unchanged. | ✅ |
| Ad hoc | Synthesizer CX standalone finding rule — Customer Experience must be generated as a standalone integrated finding when CX signals are present; may not be dissolved into Delivery Operations or Project Governance. Cross-reference appropriate; consolidation prohibited. Added to SYNTHESIZER_PROMPT section 2. | ✅ |
| Ad hoc | Synthesizer Confirmed notation rule — CONFIRMED-labeled figure in economic_impact must be a dollar amount or omitted; rates ($/hr), percentages, and target figures must use DERIVED label with inline calculation. Prevents rate card values and gap percentages from appearing as CONFIRMED exposure. Added to SYNTHESIZER_PROMPT section 2. | ✅ |
| Ad hoc | get_file_type() bug fix — function only parsed legacy {engagement_id}_{type}_{desc} format; Interview_/Doc_ prefixed files all fell through to 'other' and were processed with generic SIGNAL_EXTRACTION_PROMPT instead of specialized prompts. Fixed to route by prefix and Doc_ stem substring. File naming convention and prompt routing table added to ARCHITECTURE.md. | ✅ |
| Backlog | Findings enhancements — pattern enforcement (422 if no contributing_ep_ids; frontend Load guard), evidence_summary (computed from pattern IDs + domain signal counts, stored, shown in panel and report), key_quotes (2–3 verbatim quotes selected by Claude from domain signal notes, stored as JSON array, shown in panel and report), confidence derivation (deterministic from pattern confidence levels: all High→High, any Hypothesis→Low, else Medium). Schema: evidence_summary TEXT + key_quotes TEXT on OPDFindings. | ✅ |
| Backlog | Roadmap enhancements — capability (what org can do once item is complete; generated by Claude, editable, shown in panel and report), economic linkage (addressing_finding_ids JSON array; Claude links items to findings at parse time; economic_impact shown under each item in phase tables and synthesized per phase in narrator roadmap_rationale), dependency mapping (depends_on JSON array of item_ids; set via edit form multi-select; prerequisites shown in phase table initiative cell and in read-only item view). Schema: capability TEXT + addressing_finding_ids TEXT + depends_on TEXT on RoadmapItems. Live DB migrated. | ✅ |
| Ad hoc | ARCHITECTURE.md created — comprehensive architecture reference for new developers and architects. Covers stack, layer responsibilities, DB schema, five-agent pipeline, detect-review-load pattern, API inventory, frontend structure, report generation, economic notation, file naming/prompt routing, key design decisions, and configuration reference. | ✅ |
| Ad hoc | Executive Snapshot — three-sentence CEO-readable block added to Executive Briefing page. Opens the briefing as the first thing a reader sees: primary diagnosis, most urgent active risk with dollar figure, what must happen this week and who owns it. JSON key executive_snapshot added inside executive_briefing in REPORT_NARRATOR_PROMPT schema and instruction block. Rendered in _build_executive_briefing() before problems block with horizontal rule separator. headline key removed from Executive Briefing entirely (subsumed by snapshot). | ✅ |
| Backlog | Multi-format file ingestion — extract_text_from_file() in document_processor.py handles .txt (built-in), .docx (python-docx, document-order paragraph+table extraction), .xlsx (openpyxl, sheet headers + tab-separated rows, cached formula values), .pdf (pdfplumber, text-based only; scanned pages skipped with DEBUG log; ValueError if all pages blank), .pptx (python-pptx, text frames + speaker notes per slide). scan_folder() filter updated from .txt-only to SUPPORTED_EXTENSIONS set. process_file() open() replaced with extract_text_from_file() call. archive_candidate_files() engagement_id prefix filter removed — Interview_/Doc_ named files now archive correctly. openpyxl, pdfplumber, python-pptx added to requirements.txt. ARCHITECTURE.md updated. Validated manually with real .docx file on E004 — 9 signals returned. | ✅ |
| Ad hoc | Phase 1 CLI layer removed — top.py, commands/, root db/, root utils/, scripts/, check_views.py, test_narrator.py, test_roadmap_extractor.py, validate_template.py, test.db, api/utils/formatting.py all deleted. Zero imports in api/ confirmed before deletion. | ✅ |
| Ad hoc | config.MODEL hardcodes fixed — agent_run.py and agents.py were hardcoding 'claude-sonnet-4-6' instead of using config.MODEL. Now correctly reads TOP_MODEL env var via config.py. | ✅ |
| Ad hoc | requirements.txt encoding fixed — file was UTF-16 LE (Write tool produces UTF-16 LE on Windows). Rewritten as UTF-8 so pip install -r requirements.txt works correctly. | ✅ |
| Ad hoc | Report fix — R-codes in Key Risks table: _resolve_rcodes() was nested inside _dependency_table() and inaccessible to _risk_table(). Extracted to module-level _resolve_initiative_codes(text, roadmap_by_id). Applied to both Risk description column (row.cells[0]) and Mitigation column (row.cells[2]). Verified on E004: all 8 rows clean across both columns. | ✅ |
| Ad hoc | Report fix — R-codes in narrator prose: _resolve_initiative_codes() was only applied to table cells, not to narrator-generated free text. Applied to all 10 prose write sites: executive_summary_opening, executive_summary_para1/2/3, engagement_overview_paragraph, root_cause_narrative, economic_impact_narrative, future_state_narrative, roadmap_rationale (per phase), executive_snapshot, and domain_analysis opening/closing paragraphs. _build_executive_briefing() and _findings_by_domain() updated to accept and thread roadmap_by_id. Prompt-level prevention added to REPORT_NARRATOR_PROMPT: SYSTEM REFERENCE CODES section instructs the model never to use R-codes in prose. Full document scan of E004 (all paragraphs + all table cells): zero R-code matches. | ✅ |
| Ad hoc | Report fix — Economic totals false positives: _parse_economic_figures() was treating lowercase "confirmed" as an economic label even when used as an adjective modifier (e.g. "$9.2M confirmed revenue"), causing the totals row to exceed the firm's total revenue. Fix 1: _is_label_context() gate — accepts only all-uppercase labels or labels preceded by a delimiter (paren/colon/dash/comma); excludes lowercase labels preceded only by whitespace. Fix 2: formula-input guard — skips any dollar amount that appears inside the parenthetical text of its winning label (e.g. "$9.2M" inside "(DERIVED: 15% × $9.2M)"), treating it as a calculation input rather than the result. Together these correctly produce ~$221K+ confirmed floor on E004 ($35K overrun + $186K bench cost; $1.84M Meridian revenue excluded as concentration risk). | ✅ |
| Ad hoc | Report behavior confirmed — Sales & Pipeline domain missing from Section 6: correct behavior. _findings_by_domain() only generates a domain section when accepted findings exist for that domain. E004 has 5 Sales & Pipeline signals but 0 accepted findings — no section is generated. No code change. | ✅ |
| Backlog | Structured economic figure fields — Session 1: Schema migrations (confirmed_revenue REAL on Clients; display_figure, display_label, figure_type, include_in_executive on OPDFindings). Repositories, models, NewEngagement.jsx, EngagementDetail.jsx updated. | ✅ |
| Backlog | Executive Display UI + pre-population — Session 2: _prepopulate_display_figure() + _format_display_figure() utilities in report_generator.py. list_findings endpoint made async with parallel Claude suggest_display_label() calls (asyncio.gather). FindingsPanel Executive Display section: figure input, label input with Claude suggestion hint, figure_type dropdown, include_in_executive checkbox, Save button. suggest_display_label() added to claude.py (max_tokens=20, CEO-readable 4–6 word label). | ✅ |
| Backlog | Report generator structured field integration — Session 3: Three Numbers That Matter block, Revenue at Risk row, and Economic Impact totals row all now source from display_figure/display_label/include_in_executive structured fields instead of parsing economic_impact free text. Shared _parse_display_figure_to_float() utility added. Orange italic placeholders (RGBColor FF8C00) rendered when fields not set. _parse_economic_figures() preserved unchanged for per-finding table rows. Tests: E002 with fields set (PASS), E002 with include_in_executive cleared (PASS), E001 regression (PASS). | ✅ |
| Backlog | Claude API timeout — `timeout=120.0` added to `AsyncAnthropic` client init in `claude.py`. Prevents indefinite hang on failed Anthropic API call. Confirmed safe: `repo.create()` in `run_agent()` is called after the Claude response, so a timeout leaves no orphaned AgentRun records. All other flows (signal extraction, parse findings, report narrator) also write nothing to DB before the call. | ✅ |
| Backlog | Schema migrations table — `schema_migrations (version TEXT, applied_at TEXT)` created in TOP.db. 9 historical ALTER TABLE migrations backfilled with approximate dates. Rule added to CLAUDE.md: every future `ALTER TABLE` must insert a row into `schema_migrations`. | ✅ |
| Backlog | Document Cleanup — (1) Remove P-codes from client-facing evidence summary: `_client_facing_evidence()` render-time transform in `report_generator.py` converts stored "Supported by P38, P39 across Domain; N signals (X confirmed, Y inferred)" to "Supported by N directly observed signals across Domain" (all-High) or "…(X directly observed, Y reported)" (mixed). No DB change. (2) Strip evidentiary labels from Future State table Metric column: removed 3-line `sourced_from` label-append in `_future_state_table()`. (3) Updated `REPORT_NARRATOR_PROMPT` benchmark instruction: prior-period confirmed value preferred over industry estimate. | ✅ |
| Backlog | Split `report_generator.py` — orchestrator only (406 lines: imports, `__init__`, `generate()`, `_output_path()`, `_build()`). All helpers, constants, and 20 section renderer methods moved to new `report_sections.py` (1,560 lines) as `ReportSectionsMixin`. `_prepopulate_display_figure` re-exported so `findings.py` import unchanged. 21/21 tests pass. | ✅ |
| Backlog | Economic Structured Fields Session A — Schema: `confirmed_figure`, `derived_figure`, `annual_drag_figure` REAL (nullable) on OPDFindings. Repository GET_ALL, INSERT_FINDING, UPDATE_FINDING and both method tuples updated. FindingCreate, FindingUpdate, FindingResponse models updated. Test fixture updated. 21/21 tests pass. | ✅ |
| Backlog | Economic Structured Fields Session B — FindingsPanel Executive Display UI: Confirmed Exposure, Derived Exposure, Annual Drag inputs. `parseDollarToFloat()` / `formatFloat()` utilities (⚠ prefix stripping, K/M/B suffix). `_suggest_economic_figures()` router helper parses `_parse_economic_figures()` once per row; `_ANNUAL_DRAG_DOMAINS` heuristic for drag suggestion. Guardrail: ⚠ warning when suggested value exceeds confirmed_revenue. Suggestions never written to DB. 21/21 tests pass. | ✅ |
| Backlog | Economic Structured Fields Session C — Report generator: `_generate_economic_chart()` pivoted to `include_in_executive + display_figure` structured fields; `_parse_display_figure_to_float()` for bar values; `figure_type` drives bar color and legend (`direct_exposure` → Direct Exposure, else → Annual Drag); omits chart if no qualifying findings. `_economic_summary_table()` per-finding rows sourced from `confirmed_figure`, `derived_figure`, `annual_drag_figure` via new `_float_to_dollar()` helper; `None` → `'—'`; dedup logic unchanged. `_parse_economic_figures()` no longer called for either function. 21/21 tests pass. | ✅ |
| Backlog | Signal Library Session 1 — `migrations/migrate_signal_library.py`: SignalLibrary table (80 signals seeded via INSERT OR IGNORE), SignalCoverage table, library_signal_id TEXT column on Signals (schema_migrations: signals_library_signal_id). `SignalLibraryRepository` (get_all, get_by_id, get_by_domain). `SignalCoverageRepository` (create, bulk_create, get_for_engagement — joins SignalLibrary for signal_name/domain/priority_tier). `next_coverage_id()` (SC prefix, 3-digit pad) added to ids.py. `SignalRepository` GET_FOR_ENGAGEMENT and INSERT_SIGNAL updated to include library_signal_id (14th column). `DEFAULT_DOMAIN = 'Delivery Operations'` added to domains.py and constants.js; all 7 hardcoded fallback strings replaced across document_processor.py, findings.py, roadmap.py, SignalPanel.jsx, FindingsPanel.jsx, RoadmapPanel.jsx. 21/21 tests pass. | ✅ |
| Backlog | Signal Library Session 2 — All 7 extraction prompts updated: `_DOMAIN_LIST` injected from `VALID_DOMAINS` (replaces hardcoded domain strings in all 6 document prompts + `SIGNAL_EXTRACTION_PROMPT`). `DOMAIN_FILTER_MAP` (8 file types → domain lists). `_build_library_block(domains)` builds compact Tier 1 signal checklist from SignalLibrary DB. Library block injected into user message per file type. Output format changed to `{"found": [...], "not_observed": [...]}` with graceful fallback to legacy array. `library_signal_id` field on matched found signals. `not_observed` validated (SL-XX prefix), stored per file, merged at engagement level (found_library_ids subtracted). `extract_signals_from_transcript()` accepts `library_block` param. `TOP_Signal_Library.md` Domain Filter Map updated with resource and delivery rows, portfolio updated to include Sales-to-Delivery Transition. 21/21 tests pass. | ✅ |
| Backlog | Signal Library — Bug fix: `load_candidates` in `signals.py` was not passing `library_signal_id` in the `signal_payload` dict, so matched signals were always written with NULL even when Claude returned the field. Added `'library_signal_id': signal.get('library_signal_id')` to the payload. Confirmed with E004: 31 signals now have `library_signal_id` populated after reprocess. | ✅ |
| Backlog | Signal Library Session 3 — `SignalCoverageRepository.bulk_create()` fixed (one `_write()` per row, sequential loop, not pre-built params list — prevents duplicate SC IDs on empty table). `delete_for_engagement()` added. `load-candidates` endpoint writes SignalCoverage rows from merged candidate `not_observed` field (non-fatal: logs warning on failure, never rolls back signal load). New `GET /{engagement_id}/signals/coverage` endpoint. `api.js` `getCoverage` added. `SignalPanel.jsx`: `coverage` + `showCoverage` state, `getCoverage` added to `fetchData()` Promise.all, collapsible Coverage Gaps section (domain-grouped, collapsed by default, signal_name + signal_id per row). | ✅ |
| Backlog | Executive Briefing Sharpness + Execution Path Section — (1) `REPORT_NARRATOR_PROMPT`: `executive_snapshot` prose style rule added (short declarative sentences ≤20 words, no compound clauses, wrong/right examples). (2) Narrator JSON schema: `execution_path_recommendation` ("internal"\|"guided"\|"partner") + `execution_path_rationale` (one engagement-specific sentence). Section instructions added for both fields with firm-size default rule (under 75 people, no transformation function → "guided"). (3) `report_sections.py`: `_execution_path_section()` renders "How This Gets Implemented" as level-2 subsection in Section 11 — all three paths shown; recommended path's first sentence is narrator rationale in bold, remaining text is template. `report_generator.py`: wired into `_build()` after next steps table. 21/21 tests pass. Validated on E004. | ✅ |
| Ad hoc | Claude API timeout — per-call `timeout=` overrides added to all `messages.create()` calls in `claude.py` that were missing them: `call_claude` (300s), `extract_signals_from_transcript` (300s), `extract_findings_from_synthesizer` (300s), `compress_narrative` (300s), `extract_roadmap_from_synthesizer` (300s), `suggest_display_label` (30s). Client-level 120s default was causing `APITimeoutError` on large case packets for Delivery Operations agent and Parse Findings. Narrator (600s) was already correct. | ✅ |
| Backlog | Narrator Quality Improvements Session 1 — Added `## Roadmap Quality Rules` section to `REPORT_NARRATOR_PROMPT` with CONDITIONALITY preamble (rules apply only when trigger is directly evidenced in findings/Synthesizer/context — prevents hallucinated rule application). Two groups: (1) Sequencing Rules: Rule 1 revenue concentration stabilization precedes growth; Rule 2 rate floor policy in Stabilize not Optimize; Rule 3 change order governance portfolio-wide from Month 1; Rule 4 confirmed AI contractual liability is Stabilize concern; Rule 5 PM attrition requires structural capacity model; Rule 6 CEO bottleneck requires structural delegation in `risk_table_rows`; Rule 12 active client escalation requires contingency planning in `priority_zero_table_rows`. (2) Dependency and Timing Rules: Rule 13 sequential data dependencies explicitly sequenced in `initiative_details` timelines. All triggers evidence-grounded (finding/Synthesizer content, not pattern names). 21/21 tests pass. Validated on E004. | ✅ |
| Backlog | Quick Wins Section — Section 10.3 in the Transformation Roadmap. Filters roadmap items where `priority == 'High'` and `effort == 'Low'`, capped at 5. Renders as a light-green-header table (Initiative | Domain | Estimated Impact) between Roadmap Overview (10.2) and the phase tables. Section omitted entirely when no qualifying items exist. `_quick_wins_table()` added to `report_sections.py`. `_SECTION_MAP` updated: `quick_wins: '10.3'`, stabilize→10.4, optimize→10.5, scale→10.6, dependencies→10.7, risks→10.8. 21/21 tests pass. Validated on E004. | ✅ |
| Backlog | Narrator Quality Improvements Session 2 — Added two new groups to `## Roadmap Quality Rules` in `REPORT_NARRATOR_PROMPT`: (1) Target and Mechanism Rules: Rule 7 scale-phase >20% targets must name mechanism or flag as undefined; Rule 8 utilization improvement must address demand conversion plan; Rule 14 concurrent utilization + hiring initiatives require a concurrency scenario note in `roadmap_rationale`. (2) Output Structure Rules: Rule 9 `success_metric` must include leading indicator + completion criterion in "Track: / Complete:" format; Rule 10 `risk_table_rows` mitigations must follow "If [trigger], then [action]" format with named actor. Added OVERLAP FLAG instruction to `SKEPTIC_PROMPT`: flags double-counted exposures with adjusted combined figure before Contradiction Report. Added OVERLAP resolution instruction to `SYNTHESIZER_PROMPT`: use Skeptic's adjusted combined exposure in Economic Summary total, not individual sums. 21/21 tests pass. Validated on E004. | ✅ |
| Ad hoc | Report output quality — Session 1 (commits 5d2878f, 90aec2e): (1) `REPORT_NARRATOR_PROMPT` — 7 changes: Executive Briefing action items constrained to one sentence (Rule 12 contingency/exposure moved to `what_it_unblocks`); ANCHOR NUMBER CONSTRAINT (exactly two economic figures across briefing + summary — acute and chronic); NO CROSS-SECTION REPETITION block with named section roles; INSIGHT FIRST and SENTENCE STRUCTURE rules added to WRITING RULES; PHASE NARRATIVES FUNCTIONAL ONLY (economic stakes replaced with one constrained forward-looking reference per phase); ECONOMIC DATA EXPLAIN ONCE + HALLUCINATION PREVENTION item 1 updated for first-mention vs subsequent-mention evidentiary labels; PRIORITY ZERO CONTENT BY LOCATION block added to `priority_zero_table_rows` instruction with Rule 12 back-reference. (2) `domain_analysis` schema collapsed from two-field `opening`/`closing` to single `narrative` field (max 3 sentences: primary finding, operational consequence, optional cross-domain connection). `_findings_by_domain()` renderer updated to match. (3) `_strip_economic_source_detail()` render-time transform in `report_sections.py` strips calculation detail from Economic Impact cells, preserving CONFIRMED/DERIVED/INFERRED label only. 21/21 tests pass. | ✅ |
| Ad hoc | Report output quality — Session 2 (commit f30251f): `REPORT_NARRATOR_PROMPT` — 3 prompt-only changes: (1) `root_cause_narrative` restructured to exactly 4 paragraphs with defined content per paragraph (core structural mechanism, primary downstream manifestation, secondary downstream manifestation, compounding infrastructure failures) and hard constraints; (2) `economic_impact_narrative` constrained to 2 sentences maximum (total exposure range + consequence of inaction); (3) `success_metric` COMPRESSION RULE added — Track and Complete statements 15 words or fewer, observable outcome only. Validated on E004: root cause 4 paragraphs ✓, economic impact 2 sentences ✓, Track statements compliant ✓. Complete statements in Optimize/Scale phases remain verbose — prompt-level compression did not reliably solve it. 21/21 tests pass. | ✅ |
| A1 | Agent Grounding Guards — architectural review (2026-04-24) found DIAGNOSTICIAN_PROMPT and DELIVERY_PROMPT had no hallucination guards while Economics/Skeptic/Synthesizer/Narrator all had explicit CRITICAL/HALLUCINATION PREVENTION blocks. Split `claude.py` (1,785 lines) into `api/services/prompts.py` (all prompt constants + AGENT_REGISTRY) and slimmed `claude.py` (call functions only, imports 5 prompts from prompts.py). GROUNDING RULE blocks added to Diagnostician, Delivery Ops, and Economics prompts simultaneously. Updated `agents.py` (2 local imports) and `patterns.py` (1 local import) to import from `prompts`. 21/21 tests pass. | ✅ |
| A2 Session 1 | Agent Review UI — backend groundwork. `case_packet.py`: `_CONF_PRIORITY` module-level constant; `_section_2_signals()` sorts within each domain High→Medium→Hypothesis (fixes inverted alphabetical-DESC from SQL layer). `agents.py`: `list_agent_runs()` post-processes each run to add `referenced_signal_ids` (bare IDs, brackets stripped) parsed from `output_full` via regex. None-safe. No schema change. 21/21 tests pass. | ✅ |
| A2 Session 2 | Agent Review UI — frontend. `AgentPanel.jsx`: fetches signals on mount (Promise.all with runs); builds `signalMap` keyed by signal_id. Referenced Signals collapsible section below full output: compact cards (ID, name, confidence badge, domain); expands to source file, quote (Quote: '...' regex with U+2014 em-dash separator confirmed), flag button. Ghost signal IDs show red error card. Flag button: pre-populates Consultant Correction textarea, appends if content exists, opens correction section, shows "Flagged ✓" after click. Hint note explains workflow. Consultant Correction moved to bottom of card (below Referenced Signals) so review flows top-to-bottom. Acceptance gate removed — shown on all runs. `agents.py` regex corrected from `\[S\d+\]` to `\bS\d{3,4}\b` (agents write bare S-codes); `dict.fromkeys()` deduplication. 21/21 tests pass. | ✅ |
| A3 Session 1 | Skeptic Recommendations — backend. `PatternRepository`: `UPDATE_CONFIDENCE` SQL + `update_confidence()` method. `patterns.py` router: wire `data.confidence` in `update_pattern()` with 422 guard (was modeled but never implemented). `prompts.py`: `DOWNGRADE_EXTRACTION_PROMPT` constant. `claude.py`: `extract_downgrade_recommendations()` — max_tokens=500, validates P-code format and confidence values, returns [] on failure. `agents.py`: `_parse_c_codes()` regex helper (deduplicates signal_ids via dict.fromkeys); `GET /skeptic/c-codes` (pure regex, no Claude); `POST /skeptic/parse-recommendations` (Claude + regex, in_engagement flag per downgrade, library name lookup for unmatched patterns). `api.js`: `getSkepticCCodes`, `parseSkepticRecommendations`. 21/21 tests pass. | ✅ |
| A3 Session 2 | Skeptic Recommendations — PatternPanel downgrade cards. "Parse Skeptic Recommendations" button (amber outline, shown only when Skeptic accepted and recs not yet fetched). Downgrade cards: P-code, pattern name, confidence arrow (High→Medium), reason sentence, Apply/Dismiss. Apply writes updated confidence via PATCH endpoint, refreshes pattern list, removes card. Dismiss removes card for session only. `in_engagement:false` cards shown greyed with disabled Apply and italic note. `visibleDowngrades` filters dismissed IDs and already-applied (current_confidence === recommended_confidence) so applied cards never reappear after reload. Empty states: no recs found, all applied/dismissed. 21/21 tests pass. | ✅ |
| A3 Session 3 | Skeptic Recommendations — SignalPanel C-code badges. `fetchData()` adds `getSkepticCCodes` to Promise.all (auto-fetches on mount, no button needed). Builds `cCodesBySignalId` map keyed by signal_id. Amber badge per C-code on each flagged signal card (⚠ C001 · factual_conflict). Click to expand full C-code text inline as monospace block; click again to collapse. Multiple C-codes per signal render as separate independently expandable badges. Bug fix: `_parse_c_codes` deduplicates signal_ids via dict.fromkeys to prevent duplicate React keys when Signal A and B reference the same signal. 21/21 tests pass. | ✅ |
| A4-Pre | Pattern Detection: Include Triggering Signal IDs in Notes — `PATTERN_DETECTION_PROMPT` updated: `notes` field now requires `Triggered by: S014, S022` as first line, followed by explanation. Uses only signal IDs from SIGNALS input — no pattern library IDs or invented codes. Prerequisite for A4 evidence chain. Bug fix committed alongside: `load_patterns` endpoint now calls `delete_unaccepted_for_engagement()` before `bulk_create()` — replaces instead of appending on re-detection. 21/21 tests pass. | ✅ |
| A4 | Evidence Traceability on Findings — `_build_evidence_chain()` added to `findings.py`: extracts S-codes from pattern notes `Triggered by:` line, resolves each to signal record, extracts verbatim quote (splits on ` Interpretation:`, strips `Quote:` prefix), caps at 8 signals with `signals_hidden` count for overflow note. `signals_by_id` dict built from `all_signals` (already fetched). `evidence_chain` key added to each candidate in `parse-synthesizer`. `FindingsPanel.jsx`: `expandedEvidence` Set state, `toggleEvidence` handler, collapsible "Evidence Chain (N patterns, N signals)" toggle at bottom of each candidate card. Expanded: pattern rows with `→ S-codes` arrows, signal rows with name · confidence · quote · [source_file], `+ N more signals not shown` when capped. Candidates only — not saved findings list. 21/21 tests pass. | ✅ |
| Backlog | Domain Maturity Scoring — `_compute_domain_scores()` in `report_sections.py`: base 5, subtract per accepted pattern (High: -1.0, Medium: -0.5, Hypothesis: -0.25) and per finding (High: -0.5, Medium: -0.25), floor at 1.0, round to 1dp. All 10 VALID_DOMAINS shown; None for zero signals+patterns+findings. `_maturity_scorecard()` renders two-column table (Domain / Maturity Score 1–5) with traffic-light cell shading (green ≥4.0 Healthy, yellow ≥3.0 Concern, red <3.0 Critical, gray No Data). Placed in Section 2 after engagement overview paragraph. `PatternRepository` added to `report_generator.py` generate(). 21/21 tests pass. Validated on E004. | ✅ |
| A5 | Signal ID Validation on Agent Output — `SignalRepository.get_ids_for_engagement()` returns set of valid signal IDs for an engagement. `list_agent_runs()` in `agents.py` fetches valid IDs once per request, adds `referenced_signal_ids` (all S-codes found in output_full) and `signal_warnings` (ghost IDs not in valid set) to each run. Ghost warnings logged at WARNING level. `AgentPanel.jsx`: yellow warning banner visible on collapsed cards between error block and output summary — shows unknown ID(s) and prompts review before accepting. Does not block acceptance. 21/21 tests pass. | ✅ |

---

## Next Steps

**Current task:** Post-Checkpoint 4 backlog work — see BACKLOG.md After Checkpoint 4 section.

---

## Test Data

| Engagement | Firm | Signals | Patterns | Agents | Findings | Roadmap |
|-----------|------|---------|----------|--------|---------|---------|
| E001 | Meridian Consulting Group | 33 | 32 | 5 accepted | 7 | 16 |
| E002 | Apex Technology Solutions | 33 | 21 | 5 accepted | 7 | 16 |
| E003 | (Fictional — Dry Run 3) | 102 | — | 5 accepted | Yes | Yes |
| E004 | (Fictional — Dry Run 4) | — | — | 5 accepted | Yes | Yes |

E001 and E002 are the primary reference engagements.
E003 is the primary report testing engagement — 102 signals, full agent sequence, findings and roadmap generated.
E004 is the Checkpoint 4 validation engagement — end-to-end dry run, all features validated.
